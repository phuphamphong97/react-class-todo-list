import { colors } from '../principles';

const taskPriority = {
  low: {
    level: 'low',
    color: colors.cyan,
  },
  medium: {
    level: 'medium',
    color: colors.gold,
  },
  high: {
    level: 'high',
    color: colors.red,
  },
};

export default taskPriority;
