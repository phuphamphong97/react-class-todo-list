const zIndexes = {
  low: 0,
  medium: 100,
  high: 1000,
};

const boxShadows = {
  narrow: `rgba(0, 0, 0, 0.15) 0 5px 10px`,
  default: `rgba(0, 0, 0, 0.15) 0 10px 20px`,
  wide: `rgba(0, 0, 0, 0.2) 0 20px 40px`,
};

export default { zIndexes, boxShadows };
