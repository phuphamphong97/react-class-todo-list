const zIndexes = {
  default: 0,
  navBar: 100,
  modal: 1000,
};

const boxShadows = {
  narrow: `rgba(0, 0, 0, 0.15) 0 10px 20px`,
  wide: `rgba(0, 0, 0, 0.2) 0 20px 40px`,
};

export { zIndexes, boxShadows };
